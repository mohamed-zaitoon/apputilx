jdk:
  - openjdk17

install:
  - ./gradlew -Pgroup=io.github.mohamed-zaitoon -Pversion=${VERSION:-1.3.0} -xlint -xlintVitalRelease -xlintVitalAnalyzeRelease :library:publishReleasePublicationToMavenLocal

env:
  GRADLE_OPTS: "-Dorg.gradle.jvmargs=-Xmx2g -Dorg.gradle.parallel=true"
  
